import { StyleSheet } from 'react-native';

export const imageStyles = StyleSheet.create({
    backgroundImage: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
        resizeMode: 'stretch'
    }
});
