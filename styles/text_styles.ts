import { StyleSheet } from 'react-native';

export const textStyles = StyleSheet.create({
  title: {
    fontSize: 60,
    fontWeight: 'bold',
    fontStyle: 'italic',
    textAlign: 'center',
    color: '#d67702',
    textShadowColor: '#7a4709',
    textShadowOffset: { width: 1, height: 1 },
    textShadowRadius: 5
  }
});
